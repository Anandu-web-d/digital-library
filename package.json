{
  "name": "digital-library",
  "version": "1.0.0",
  "description": "Digital Library - AI-Powered Library Platform",
  "private": true,
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "npx serve frontend/dist -s -l 10000",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.22",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.17"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}